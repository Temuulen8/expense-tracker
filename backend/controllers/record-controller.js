const sql = require("../config/db");

const getAllRecord = (req, res) => {
  res.status(200).json({});
};

const createRecord = () => {};
const updateRecord = () => {};
const deleteRecord = () => {};

module.exports = {
  getAllRecord,
  createRecord,
  updateRecord,
  deleteRecord,
};
